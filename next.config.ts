import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["t4.ftcdn.net","res.cloudinary.com"], // Add the external domain here
  },
};

export default nextConfig;
